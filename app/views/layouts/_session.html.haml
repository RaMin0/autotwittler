- if user_signed_in?
  Logged in as #{link_to current_user.name, profile_path}.
  = link_to 'Logout', destroy_user_session_path, method: :delete
- else
  = link_to 'Login with Twitter', user_omniauth_authorize_path(:twitter)
